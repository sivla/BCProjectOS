# Design

The fixture is local-only, synthetic and read-only validated. Both profiles use the same record contract; implementation and support-only differ only by controlled profile metadata. No customer IDs, external systems or runtime paths are embedded.
