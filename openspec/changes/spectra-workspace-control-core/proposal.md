## Why

Spectra 1.0 requires one product core that can operate both implementation and support-only workspaces.

## What Changes

Add the next release delta after published `0.1.0-beta.1`: a synthetic, non-installable control fixture with canonical project, ticket, risk, decision, phase, work-package and role records for both profiles. The evidence supports `0.2.0-alpha.1` as a proposed candidate because it adds a backward-compatible control capability; it is not a publication claim.
