## Why

Spectra 1.0 requires one product core that can operate both implementation and support-only workspaces.

## What Changes

Add a synthetic, non-installable control fixture with canonical project, ticket, risk, decision, phase, work-package and role records for both profiles. No release version is claimed.
