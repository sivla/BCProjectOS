## Why

Spectra 1.0 needs a controlled local intake path that preserves original bytes and fails closed on unknown or unsafe input.

## What Changes

Add the next release delta after `0.3.0-alpha.1`: a synthetic-only file-intake generator, dry-run/apply entrypoint, metadata validator, quarantine contract and negative safety matrix. This supports proposed `0.4.0-alpha.1`; no live integrations or publication claim.
