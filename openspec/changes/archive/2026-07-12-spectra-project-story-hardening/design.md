# Design

The generator creates non-empty Markdown pages and deterministic story JSON. The validator requires a separate closing comment, acceptance criteria, evidence and working comment for closed tickets; checks chronology, page parents, worklogs, hypercare and graph references. `0.6.0-alpha.1` remains below 1.0 because pilot/RC and full CLI evidence are still open.
